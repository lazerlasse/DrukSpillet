<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:DrukSpillet.ViewModels.NeverHaveIEver"
             xmlns:pages="clr-namespace:DrukSpillet.Pages"
             x:Class="DrukSpillet.Pages.NeverHaveIEver.NeverHaveIEverStartPage"
             x:DataType="viewmodels:NeverHaveIEverStartPageViewModel"
             Title="{Binding Title}">
    <VerticalStackLayout>
        <Label 
            Text="{Binding Title}"
            VerticalOptions="Center" 
            HorizontalOptions="Center" />
    </VerticalStackLayout>
</ContentPage>