<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:DrukSpillet.ViewModels.TruthOrDare"
             xmlns:pages="clr-namespace:DrukSpillet.Pages"
             x:Class="DrukSpillet.Pages.TruthOrDare.TruthOrDareStartPage"
             x:DataType="viewmodels:TruthOrDareStartPageViewModel"
             Title="{Binding Title}">
    <VerticalStackLayout>
        <Label 
            Text="{Binding Title}"
            VerticalOptions="Center" 
            HorizontalOptions="Center" />
    </VerticalStackLayout>
</ContentPage>